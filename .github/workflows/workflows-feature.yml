name: workflows

on:
  push:
    branches:
      - "feature/*"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
  tests-coverage:
    uses: ./.github/workflows/tests-coverage.yml
